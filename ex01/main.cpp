#include "PhoneBook.hpp"

int main()
{
	while (strncmp(argumento, "EXIT"))
	{
		PhoneBook phonebook;

		if (!strncmp(argumento, "ADD"))
			phonebook.addContact(this.contacts);
		else if (!strncmp(argumento, "SEARCH"))
			phonebook.searchContact();
		else if
			return (1);
	}
	return (0);
}
